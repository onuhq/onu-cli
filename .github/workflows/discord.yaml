name: post to discord

on: push

jobs:
  publish-gpr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: curl -X POST -H "Content-Type: application/json" -d '{"embeds":[{"type":"rich", "title":"test from gh actions"}]}' ${{secrets.DISCORD_WEBHOOK_URL}}
